package uuid

import "github.com/hamhockwarlock/id-comparison/db"

func (u *db.Wrapper) fetch(uuid string) (User, error) {

	return User{}, nil
}

// func Create(user User) (string, error) {
// 	return "", nil
// }
